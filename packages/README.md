# packages

Reserved for shared packages consumed by both `apps/web` and `apps/api`
(e.g. shared Zod schemas, TypeScript types, UI tokens, or a client SDK).

Each package should be an npm workspace with its own `package.json`.
